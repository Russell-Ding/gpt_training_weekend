# @package _global_.data

# Shakespeare dataset configuration
dataset_name: "shakespeare"
train_path: "data/shakespeare/train.bin"
val_path: "data/shakespeare/val.bin"
stride: 256  # Stride for sliding window (no overlap)
